## Virtual Server Demo
Drag and drop this folder into HyperHost.

The client script will be hosted, then make a subsequent request to the virtual server.  

This demo demonstrates connecting to the virtual server, pulling from NPM, and how custom node modules can be built.
